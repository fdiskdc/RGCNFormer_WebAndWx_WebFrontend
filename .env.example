# API 基础 URL（默认使用当前域名下的 /api/v1）
VITE_API_BASE_URL=/api/v1

# 旧版预测端点（默认使用当前域名下的 /api/predict）
VITE_LEGACY_PREDICT_URL=/api/predict

# Vite 开发服务器代理目标（默认 http://localhost:8000）
VITE_PROXY_TARGET=http://localhost:8000
